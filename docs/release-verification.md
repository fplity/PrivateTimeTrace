# 1.0.0 安装版验证

日期：2026-09-14。由主会话直接构建和验证，不是独立测试员验收，也不替代第三方安全审计。

环境：Windows 11 家庭中文版 x64，系统 Build 26200；.NET SDK 10.0.401。此结果仅对应本机环境。

## 本地执行结果

| 检查 | 结果 |
| --- | --- |
| 核心数据 / 统计 | 21 项检查通过 |
| 构建 | Release x64，自包含 .NET 和 Windows App SDK，产品版本 1.0.0.0 |
| 安装包 | NSIS 3.12；当前用户权限，93,371,676 字节 |
| 免安装 ZIP | 97,727,123 字节，600 个文件；未包含数据库、凭据、调试符号或日志 |
| 许可完整性 | 26 个解析依赖包（含构建期包）；64 个原始许可 / 声明 / 元数据文件的 SHA-256 核对一致，另附 NSIS 与 Windows 二进制条款 |
| 静默安装 | 未传接受许可参数返回 3，未安装任何文件；正常传参安装成功 |
| 安装注册 | 应用文件、图标、许可文件、开始菜单目标、HKCU 卸载信息正确 |
| 安装后的 UI | 两种主题 × 两个图表各两种模式，共 8 个组合均通过；偏好重启恢复 |
| 安装后的计时 | 输入主题 → 开始 → 关闭 → 恢复 → 结束，恰好新增一条记录 |
| 统计范围 | 日 / 周 / 月 / 年 / 全部实际切换通过 |
| 窄窗口 | 按 950 × 700 逻辑尺寸调整，实际滚动到页底后风格入口仍可切换 |
| 占用保护 | 目标应用运行时，安装与卸载均返回 6，不强制终止应用 |
| 覆盖升级 | 隔离数据库的记录、活动状态、主题 / 图表偏好保持一致 |
| 卸载 | 移除程序、开始菜单和卸载注册；额外用户文件与学习数据仍保留 |
| 正式数据 | 测试只读核对原正式数据库指纹，未改变正式数据；未向正式库写入演示数据 |
| 安装向导外观 | 中文欢迎页、MIT / 运行库许可页、原始 Windows SDK RTF 许可页已实际查看，图标正确 |
| NuGet 公告检查 | 本次 `--vulnerable --include-transitive` 未返回已知漏洞包，不代表所有代码已经过安全审计 |
| 签名 | 未进行商业 Authenticode 签名；未修改系统信任证书或安全防护设置 |

最终包测试证据保存在本地 `artifacts/qa/installer-verification-final-2.log` 和安装向导 / 实际窗口截图。隔离库留作复核，不提交到仓库。测试安装和临时快捷方式已正常卸载。

首轮窄窗口测试沿用了固定物理像素，在当前缩放比例下页面不需要滚动，因此脚本报错。随后将验证改为基于窗口 DPI 的逻辑尺寸，并明确要求出现可滚动视口；完整流程重跑通过，未通过跳过滚动来掩盖问题。

最终包的一次复验中，页面中途进入了主题记录详情，图表查询因此中断。保留诊断快照后，减少验证脚本抢占前台的行为，在同一安装包上重跑完整流程通过；未跳过任何显示组合、滚动或数据保留断言。

## 校验值

```text
8e880a883b7b163bc0c67035368d2b3a7a3cfca58a93774d11e6ba1e5c69500f  PrivateTimeTrace-1.0.0-win-x64-Setup.exe
8e6da9edd2c9674aed135dca60c95fd48d13a3adb04d81ee445579b4090bd214  PrivateTimeTrace-1.0.0-win-x64.zip
```

## 未覆盖范围

未验证所有 Windows 10 版本、ARM64 / x86、全部显卡、高对比配色、远程桌面或长期运行。没有同步、账号、自动更新或应用级数据库加密。CI 的构建结果须与本机原生 UI 验证分开理解。
