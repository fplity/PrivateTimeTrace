# 双风格桌面版验证记录

日期：2026-09-14。验证由主会话直接执行，不是独立测试员验收。

## 交付

发布程序：`artifacts\TimeTrace-win-x64\PrivateTimeTrace.exe`，Windows x64 自包含、非 MSIX。

应用 DLL SHA-256：`270D39E8CA41366655066906395EA68A99843603AEB6D2C0B5302DFED5D8876A`。

已关闭旧版窗口并打开最终发布程序。最终窗口标题为「时间迹」；右上角两种风格、计时按钮和两个图表的四个切换按钮均通过原生 UI Automation 检查为可用且可见。

## 已执行的检查

| 项目 | 结果 / 证据 |
| --- | --- |
| 数据与统计 | 21 项通过，`artifacts\qa\core-checks.log`；涵盖跨午夜、五周期总计 / 趋势 / 主题一致性、完整历史、偏好回读、活动计时恢复、原子保存、定向删除、旧数据库迁移 |
| 最终发布版 UI | 全部 8 种风格 / 双图表组合通过，`artifacts\qa\release-ui-3.log` |
| 偏好重启恢复 | 霜白玻璃 + 趋势柱状 + 主题折线在重启后仍选中 |
| 完整计时链路 | UI 输入主题、开始、关闭、恢复、结束，记录数只增加 1，主题正确，活动会话清除 |
| 五种周期 | 日、周、月、年、全部均通过原生窗口切换检查 |
| 窄窗口 | 1280 × 1100 物理像素：折叠导航文字、双图表竖排；滚动到底后右上角风格开关仍可用 |
| 生命周期 | 最终版本连续 6 次恢复已有活动计时、关闭，全部退出码为 0；`artifacts\qa\lifecycle.log` |
| 空状态 | 两个图表同时显示空状态，计时入口仍可用；`artifacts\qa\release-empty.png` |
| 删除确认 | 在隔离库中，取消保持 26 条，确认后只删除目标测试记录，剩 25 条 |
| 主题下钻 | 点击「Java 核心」图表数据点，实际切换到对应主题的记录列表 |
| 正式数据 | 更新前后正式学习记录及活动会话的内容指纹一致，未写入演示 / 测试记录 |

## 真实窗口截图

- `artifacts\qa\release-liquid.png`：液态流光，趋势折线 + 主题柱状，隔离演示数据。
- `artifacts\qa\release-frosted.png`：霜白玻璃，趋势柱状 + 主题折线，隔离演示数据。
- `artifacts\qa\release-narrow-scrolled.png`：小窗口滚动后，固定风格切换仍然可见。
- `artifacts\qa\final-running.png`：最终发布版读取正式本地数据库的实际窗口。

这些图片来自原生窗口捕获，不是概念效果图。

## 修订记录与验证边界

交互检查发现主题输入默认延迟到失焦才回写，已改为逐次更新。高 DPI 默认窗口尺寸与窄窗口排布已调整。风格与图表同时支持鼠标、键盘 / 辅助功能选择事件。

早期候选版在重启检查中出现过原生窗口异常。后续补充关闭时停止 UI 计时刷新、避免在未加载 / 正在关闭的控件上启动动画，以及延迟创建 UI 设置对象。最终完整 UI 回归和 6 轮生命周期检查均通过，未再次复现；不能据此宣称完成长期稳定性或全部硬件兼容验证。

本次只验证本机 Windows 环境及本地单机功能。未验证所有高对比配色、远程桌面、ARM64、触屏、其他显卡 / 系统组合，也没有新增同步、账号或云服务。
